{
  "name": "frontendframework",
  "version": "1.0.0",
  "description": "small framework for learning front end libraries",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf build/ && parcel build index.js -d build/",
    "start": "rm -rf .cache && rm -rf dist && parcel serve index.html",
    "docs": "vuepress dev book",
    "build:docs": "vuepress build book && rm -rf ./docs && mv ./book/.vuepress/dist ./docs"
  },
  "author": "brandesdavid",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "parcel": "^2.10.3",
    "vuepress": "^1.9.10"
  },
  "dependencies": {
    "parcel-bundler": "^1.12.5",
    "snabbdom": "^3.5.1"
  }
}


